const form=document.querySelector('#form'),input=document.querySelector('#input'),messages=document.querySelector('#messages');function add(text,cls){const d=document.createElement('div');d.className=`msg ${cls}`;d.textContent=text;messages.append(d);messages.scrollTop=messages.scrollHeight}form.onsubmit=e=>{e.preventDefault();const text=input.value.trim();if(!text)return;add(text,'user');input.value='';setTimeout(()=>add('This is a front-end chat interface demo. Connect a secure server-side AI endpoint to make responses dynamic.',''),400)};