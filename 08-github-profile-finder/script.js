const form=document.querySelector('#form'),out=document.querySelector('#output');form.addEventListener('submit',async e=>{e.preventDefault();const user=document.querySelector('#user').value.trim();if(!user)return;out.textContent='Loading…';try{const d=await fetch(`https://api.github.com/users/${encodeURIComponent(user)}`).then(r=>{if(!r.ok)throw Error('User not found');return r.json()});out.innerHTML=`<div class="profile"><img src="${d.avatar_url}" alt=""><h2>${d.name||d.login}</h2><p>${d.bio||'No bio provided.'}</p><p>Repos: ${d.public_repos} · Followers: ${d.followers}</p><a href="${d.html_url}" target="_blank" rel="noreferrer">GitHub profile</a></div>`}catch(err){out.textContent=err.message||'Request failed.'}});