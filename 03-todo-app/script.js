const form=document.querySelector('#form'),input=document.querySelector('#task'),list=document.querySelector('#list');let tasks=JSON.parse(localStorage.getItem('tasks')||'[]');function render(){list.innerHTML='';tasks.forEach((t,i)=>{const li=document.createElement('li');const span=document.createElement('span');span.className=`text ${t.done?'done':''}`;span.textContent=t.text;span.onclick=()=>{t.done=!t.done;save()};const del=document.createElement('button');del.textContent='Delete';del.onclick=()=>{tasks.splice(i,1);save()};li.append(span,del);list.append(li)})}function save(){localStorage.setItem('tasks',JSON.stringify(tasks));render()}form.addEventListener('submit',e=>{e.preventDefault();if(input.value.trim()){tasks.push({text:input.value.trim(),done:false});input.value='';save()}});render();