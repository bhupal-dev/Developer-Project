const input=document.querySelector('#city'),btn=document.querySelector('#search'),out=document.querySelector('#output');
async function load(){const city=input.value.trim();if(!city){out.textContent='Enter a city.';return}out.textContent='Loading…';try{const g=await fetch(`https://geocoding-api.open-meteo.com/v1/search?name=${encodeURIComponent(city)}&count=1`).then(r=>r.json());if(!g.results?.length)throw new Error('City not found');const p=g.results[0];const w=await fetch(`https://api.open-meteo.com/v1/forecast?latitude=${p.latitude}&longitude=${p.longitude}&current=temperature_2m,relative_humidity_2m,wind_speed_10m&timezone=auto`).then(r=>r.json());out.innerHTML=`<div class="weather"><div class="card"><b>${p.name}</b><br>${p.country||''}</div><div class="card">Temperature<br><strong>${w.current.temperature_2m}°C</strong></div><div class="card">Humidity<br><strong>${w.current.relative_humidity_2m}%</strong></div><div class="card">Wind<br><strong>${w.current.wind_speed_10m} km/h</strong></div></div>`}catch(e){out.textContent=e.message||'Unable to load weather.'}}
btn.addEventListener('click',load);input.addEventListener('keydown',e=>{if(e.key==='Enter')load()});