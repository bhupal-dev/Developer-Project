const form=document.querySelector('#form'),result=document.querySelector('#result');form.addEventListener('submit',async e=>{e.preventDefault();const amount=Number(document.querySelector('#amount').value),from=document.querySelector('#from').value,to=document.querySelector('#to').value;if(!amount)return;result.textContent='Loading…';try{const d=await fetch(`https://api.frankfurter.app/latest?amount=${amount}&from=${from}&to=${to}`).then(r=>r.json());result.textContent=`${amount} ${from} = ${d.rates[to]} ${to}`}catch{result.textContent='Conversion unavailable.'}});